{
  "mcpServers": {
    "ddb-explorer": {
      "command": "uv",
      "args": ["run", "--directory", "/absolute/path/to/ddb-explorer-mcp", "ddb-explorer-mcp"],
      "env": {
        "AWS_PROFILE": "default",
        "AWS_REGION": "us-east-2"
      }
    }
  }
}
